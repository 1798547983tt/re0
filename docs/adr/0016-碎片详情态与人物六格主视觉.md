# 碎片详情态与人物六格主视觉

点击六槽位碎片后，舞台不被替换或遮蔽为模态窗口；右侧固定详情栏滑入，显示碎片圆标、编号、标题、状态描述和只读变量格式化值，右上角返回箭头恢复主界面。顶部人物轨道在详情态隐藏，返回时恢复，右下角保留只读激活胶囊。

每个当前人物的舞台视觉优先使用一张无文字的六格构图图，再由六个固定多边形窗口裁切；素材加载按人物懒加载，缺少专属构图时回退到项目生成的 Re:0 档案背景与同一 registry 头像。这样可以保持六块碎片的光照、角色比例和边缘描线一致，同时不把状态文字烘焙进图片。
