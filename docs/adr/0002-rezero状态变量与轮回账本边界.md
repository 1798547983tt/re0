# Re:Zero 状态变量与轮回账本边界

本项目将可变状态固定为“世界、主角档案、轮回、关系、事件、线索、资产、规则”八个领域；主角档案在初始化时锁定且不可切换。菜月昴的明确实际死亡是世界重启的唯一触发事件：每个唯一死亡事件使世界重启次数与轮回编号各增加一次，世界状态恢复到存档点，而死亡记录、计数器和菜月昴带回的少量遗留情报属于跨轮回账本，不随快照回滚。这样可以支持任意原作人物或原创角色作为主角，同时保持时间线、轮回分支和状态层与世界书资料的职责分离。
