# 正文正则内嵌核心并区分流式状态

单个导入文件携带成对的“生成中”与“完成” Regex 定义，并内嵌解析器、配置、样式和交互核心；远程只加载不可变的图片和字体。运行基线为 SillyTavern 1.17+ 及检测可用的 Tavern Helper；依赖或网络失败时降级为安全文本/CSS 呈现。成对定义可避免把未完整输出误判为完成，内嵌核心也避免悬空的远程可执行代码；代价是导入产物更大，且核心变化后必须确定性重建并重新导入。
