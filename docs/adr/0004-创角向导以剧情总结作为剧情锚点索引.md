# 创角向导以剧情总结作为剧情锚点索引

创角向导的卷、事件标题和事件时间统一从仓库 `剧情总结/第01卷.txt` 至 `第39卷.txt` 的事件索引生成，而不是在前端重复维护一套剧情副本。这样可以保留项目对规范日期、轮回分支、历史回溯和资料性质标记的既有边界；前端只保存用户选中的剧情锚点，生产实现仍需把完整主角档案映射到 ZOD 状态协议。

**Status**: accepted

**Consequences**:

- 修改卷级标题、事件节点或事件时间时，先更新 `剧情总结/`，再重新生成向导索引。
- 前端不得把事件时间当作官方绝对日期，也不得把剧情锚点当作死亡回归存档点。
