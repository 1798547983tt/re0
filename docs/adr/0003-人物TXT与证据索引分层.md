# 人物 TXT 与证据索引分层

人物 TXT 采用“角色内容层”边界：只保留能直接驱动说话、行动、关系、能力限制、阶段和阶位条件的内容；卷章命中、别名归并、来源类型、置信度与审计说明统一留在独立证据索引中。这样 SillyTavern 导入时不会把维护字段误当成角色设定，同时仍能通过 `data/character_evidence.json` 回查原文；代价是维护者需要分别查看角色文件和证据报告。
