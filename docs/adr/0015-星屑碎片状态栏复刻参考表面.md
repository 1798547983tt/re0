# 星屑碎片状态栏复刻参考表面

当前发布表面以用户提供的参考图作为几何和交互基准：打开后进入全屏 16:9 碎片主界面，保留左侧固定导航、顶部人物轨道、六个不规则多边形碎片、右上角入口/返回和左下角身份标记。原有卡片式面板、仪表盘式 Hero、日夜切换和移动端抽屉不再作为当前表面；它们仅保留在 Git 历史中。视觉素材、角色文案和变量内容替换为 Re:0 项目内容，但不改变参考图的空间层级、编号节奏、描边语言和点击反馈。

这样做是为了满足“复刻”而不是“同主题再设计”的验收方式。代价是六槽位舞台需要固定几何和响应式缩放策略，复杂状态必须在固定槽位内折叠，而不能通过自由增删卡片解决。
