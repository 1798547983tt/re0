# Re:Zero 全变量状态栏真实运行时交接

## 发布候选身份

- 源码基线：`4a51a897e4085acffd9c54674bfd8bf33223810f`
- 导入产物：`dist/regex-Re0·全变量状态栏.json`
- 产物大小：162064 bytes
- 产物 SHA-256：`a3cb9c86db2f64259c4a0aa9b1c5c7af0403f681200d82f5ae7ae509fc9f53ec`
- 图像固定提交：`75d39874e8b6246a0d5f9bd45779441cdaf743cf`
- 离线 QA：`reports/statusbar-offline-qa.md`

交接文档之后的提交只能改文档或计划；如果导入 JSON、任何状态栏源码、打包器或图像发生变化，必须重新计算产物哈希，并以新哈希重新完成真实宿主验收。

## 图像身份

| 文件 | bytes | SHA-256 |
| --- | ---: | --- |
| `day-archive-wide.webp` | 168182 | `3175f1c64779bf118e9580088f5c542bd80af841ebb4919606d07d998cd31be4` |
| `day-archive-mobile.webp` | 122826 | `1087f3920eed0f1611fdaf7ee0925e3aa7fc23476e03fc574c6bb3fdf8c30963` |
| `night-tea-wide.webp` | 147802 | `d4238833d0bfdfd77d57cfcd5364876d799b0b54816d795c5e41d447e0f7dfd5` |
| `night-tea-mobile.webp` | 187468 | `85a571f9d7c976411dcb712cc47007facfe7ac3330b2899cfba391116bb39845` |

2026-08-15 对四个固定 GitHub Raw URL 重新下载，均返回 HTTP 200，下载字节数与上表一致。

2026-08-15 的背景与收纳复验确认：场景图以 `opacity: 1` 进入状态栏绘制层，文字区域改用局部半透明遮罩；日间/夜间宽图与移动图均可直接辨认。复验覆盖 1180 × 900 与 320 × 900 CSS px，页面无横向溢出，最新预览来源的 console 为空。桌面概览核心组展开时状态栏高 842px，切换到目标与警示组为 725px；320px 新聊天展开详情但未展开信息组时为 653px。

同日的交互与透明层复验进一步确认：iframe `body`、状态栏外壳、场景头部与常驻概览的计算背景均为透明；详情面板只保留 22% 局部衬底。整栏详情常驻并用 `1fr ↔ 0fr` 过渡，点击头部或概览非控件区域开合；头像、日夜与详情控件不误触开合。手风琴在原节点内使用 `auto 0fr ↔ auto 1fr` 过渡，标签、筛选和分页只更新详情面板，不再重建整棵状态栏。1180px 下收起为 176px、展开但不打开信息组为 636px、概览核心组为 842px；320px 下日间收起为 209px、展开且信息组全收起为 653px，横向溢出均为 0。生产图片 URL 仍固定到上表提交，移动日间图实测 `opacity: 1` 且 console 无 warning/error。

## 导入与取证步骤

1. 在真实 SillyTavern 环境启用角色卡所需的 Tavern Helper、MVU loader、ZOD 脚本与变量世界书。
2. 导入上方精确路径中的 regex JSON，并记录导入文件 SHA-256；不要从源码现场重打包后直接沿用本报告结论。
3. 打开开发者工具，清空 console 与 network；记录 SillyTavern、Tavern Helper、MVU 实际版本。
4. 对下面每项保存可复查证据：操作前后消息 ID、界面截图、相关网络请求、console 输出，以及是否发生重复挂载。
5. 只有全部宿主相关项目通过且证据明确写出产物 SHA-256，才可标记“真实 SillyTavern 已验收”。

## 必测清单

当前状态统一为“待真实宿主验证”。

- [ ] 新聊天首次生成后，状态栏只在 AI 输出消息末尾出现一次，且保留正文和 `<UpdateVariable>`。
- [ ] 初始化后读取当前消息楼层的 `stat_data`，不是全局变量或相邻楼层。
- [ ] `VARIABLE_UPDATE_ENDED` 后刷新一次；不写入变量、不触发新的更新回路。
- [ ] 左右滑动、编辑消息、重新生成与重渲染后无重复 mount、重复监听或遗留抽屉。
- [ ] 页面刷新后主题偏好、活动分区、信息组与列表显示量按聊天恢复；新聊天使用紧凑默认值；共享头像保留、聊天覆盖正确隔离。
- [ ] 点击场景头部或常驻概览的非控件区域可顺滑开合详情；头像、日夜、标签、筛选、分页和抽屉操作不误触整栏开合，连续快速点击不产生重复面板或未处理异常。
- [ ] 自动日间、自动夜间、手动覆盖与恢复自动四条路径正确；黎明/傍晚过渡不改变世界时间。
- [ ] 主角及关系人物可上传本地图、裁剪/缩放、保存 WebP、移除，并可使用有效 HTTPS URL。
- [ ] 失效头像 URL 回退姓名首字；HTTP、`data:` 与 `javascript:` URL 被拒绝。
- [ ] 320px 宿主容器中九个分区保持单行横滑，信息组默认收起；浏览器 200% 缩放、键盘、Escape/焦点返回与减少动态模式可用，无页面级横向溢出。
- [ ] 远程背景图失败时仍显示渐变与完整数据；四个固定 URL 成功时选择正确宽图/移动图。
- [ ] 消息 iframe 外层背景为透明，宿主主题能透出；场景图成功时头部、常驻概览与详情留白不再被整块黑底或高强度全幅蒙层遮住。
- [ ] Tavern Helper 读取失败但 MVU 可用时正确降级；两者都缺失时显示可重试错误，不猜测宿主私有 DOM。
- [ ] 空、畸形、旧数据与未知字段不会破坏其余分区；敏感存档快照和死亡记录默认折叠。
- [ ] 全程 console 无未处理异常，network 无非预期写请求，状态栏未调用变量写 API。

## 离线已确认、宿主仍待确认的边界

离线测试已经覆盖 172 个声明叶路径、九个分区、只读 API 表面、包内模块顺序、恶意文本、头像存储逻辑、响应式布局及降级模型。它不能证明某一安装中的扩展组合、消息楼层语义、MVU 事件时序、iframe 生命周期或聊天切换行为，因此这些项目不得由静态测试推断为通过。

本分支另外将两个 regex 打包器的输入行尾规范化为 LF，并机械重生成旧创角向导包；旧包与新包在统一 CRLF/LF 后语义完全一致。该变更用于保证 Windows 上的 `--check` 可复现，不改变创角界面逻辑。
