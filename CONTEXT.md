# 正传剧情时间线

本项目把本地 `正传/` 第01–39卷整理为可检索的事件时间线。总结以用户指定的菜月昴初次穿越为锚点，并把原文相对时间、轮回重置和历史回溯分开记录。

## 时间线语言

**规范日期**：按本项目专用演算历写出的完整年月日。每年12个月、每月30天；D+0 为魔女历1000年01月01日。规范日期是编辑演算，不是原作明示的绝对日期。

**世界时间**：当前轮回中世界实际流逝的日期与时段。死亡回归会把世界时间重置到存档点，不把菜月昴的累计记忆时间加回世界时间。

**轮回分支**：由一次存档点重置产生的失败或尝试路线。分支日期沿用重置点的规范日期，并以分支编号区分。

**时间层**：事件所属的叙事层，取 `主线`、`轮回分支`、`历史回溯` 或 `试炼幻境`。历史和试炼不推进主线世界时间。

**事件节点**：一个具有清楚起因、连续经过和阶段性结果的剧情单元；同一行动中重复的战斗或死亡可合并，但改变策略或揭示新信息的轮回应单独列出。

**来源定位**：卷文件名、章节标题和近似行号范围的组合，用于回查，不代替原文引用。

**卷级标题**：每个TXT只保留一个 `<卷标题>...</卷标题>` 闭合标签。事件正文不再套XML字段标签，改用普通小标题和空行分段；这不是删减事件字段，而是把字段从标签语法改成可读排版。

**审查标准**：每个事件必须能回答“谁因为什么在何时何地采取了什么行动，造成什么结果”；重点核对行动者、因果方向、死亡回归是否推进日期、历史/试炼是否误并主线、来源章节是否覆盖实际叙述。若原文没有绝对日期，规范日期仍须标为编辑演算或历史估算。

## 词汇约定

**魔女历**：输出中的项目日期前缀；原文没有提供完整魔女历换算表，因此除明确的用户锚点外，日期均须标为编辑演算或历史估算。
_避免_: 把规范日期称为原作明示日期。

**时段**：只使用黎明、清晨、上午、正午、下午、傍晚、夜间、深夜、凌晨、时段未详等中文粗粒度词，不反推不存在的钟点。
_避免_: 把阳日七时、阳日八时等原文标签擅自换算成地球时钟分钟。

## 人物档案词汇

**人物档案**：以一个可识别的具名个体为单位的 TXT 人设文件；同一人的化名、译名和阶段状态合并在同一文件中。

**角色内容层**：人物 TXT 中直接影响扮演的身份、性格、欲望、关系、能力、限制、阶段、阶位条件和行为规则；它应能脱离维护工具单独使用。

**证据索引层**：维护用的卷章命中、别名归并、来源类型和置信度数据；它支持回查与审阅，但不嵌入角色 TXT，避免把审计说明当成角色台词或行动规则。

**阶段性人设**：只有身份、记忆、身体、能力或阵营发生实质变化时才新增的阶段段落；历史回溯、轮回分支和试炼幻境必须标明时间层，不自动视为主线成长。

**战力等阶**：项目人物战斗能力的七级尺度，1阶最低、7阶最高；每阶再分上位与下位。战力参考的 T0–T6 依次映射为 7–1 阶，形态、场地、相性和权能是否启用必须单独说明。

**资料置信度**：证据索引层的维护标记，取高、中、低；正文孤证、战力表孤证或推定用于审阅和回查，不写进角色内容层，也不能冒充原作定论。

## 变量系统词汇

**主角档案**：当前聊天初始化时锁定、且由变量系统追踪完整状态的唯一主角；可以是任意原作人物或原创角色，聊天期间不可切换。
_避免_：视角角色、玩家角色、固定主角

**世界重启**：当前运行世界中的菜月昴实际死亡后，世界状态回到死亡回归存档点的过程；无论主角档案是谁，每次实际死亡事件只使重启次数增加一次。
_避免_：读档、主角复活

**死亡回归存档点**：世界重启时用于恢复可变世界状态的完整快照；其推进由剧情机制决定，不是角色可以主动选择的能力。
_避免_：玩家存档、手动检查点

**跨轮回账本**：不随世界重启回滚的系统状态，只包含世界重启次数、菜月昴死亡记录及其精简遗留情报。
_避免_：角色知识库、世界书资料

**菜月昴死亡记录**：每次触发世界重启前追加到跨轮回账本的死亡事件，记录时间、地点、直接原因、经过、相关人物、原轮回分支和精简遗留情报。
_避免_：死亡统计、失败路线摘要

**状态仪表**：用于表达当前生命、体力、魔力和精神稳定程度的 0–100 叙事读数；它不是原作官方计量，也不单独决定行动结果或战斗胜负。
_避免_：官方属性值、战力数值

**状态协议**：只保存当前可变状态、跨轮回账本和运行时锁定信息；原作资料、来源和角色知识由世界书负责。
_避免_：人物资料库、证据账本

**状态栏**：贴近聊天消息、用于反复扫读当前状态的只读两层界面；常驻概览展示高频信息，分区详情通过选项切换完整浏览各变量领域，但不写入状态协议，也不承担创角或世界资料维护。
_避免_：控制中心、角色编辑器、变量修改器、把全部变量同时平铺的长页面

**变量更新回执**：将每轮 `<UpdateVariable>` 的生成中状态或完整更新依据与 JSON Patch 呈现为消息内只读记录的双阶段界面；它只改变显示，不验证、执行或改写补丁，也不替代状态栏。
_避免_：状态栏、变量编辑器、补丁校验器、把“已生成”写成“已执行”

**完整状态映射**：状态协议中每个正式字段都有明确的展示位置，动态记录可逐项浏览，协议允许的未知字段可在诊断视图中回查；默认折叠敏感或庞大的数据仍属于完整映射。
_避免_：只展示当前非空字段、把原始状态树直接平铺

**状态栏头像**：状态栏中主角档案和关系人物的本地视觉标识；没有自定义图像时显示姓名首字，用户可从本地文件或图片 URL 替换。
_避免_：把头像当作人物资料来源、把固定作品角色图当作当前主角身份

**状态栏界面模式**：状态栏的日间或夜间完整视觉呈现，默认随世界时段选择，并允许用户在界面中覆盖或恢复自动选择；模式变化包含场景、层次和动态表现，但不修改世界时间。
_避免_：只替换色板、把界面选择写入状态协议

**状态栏场景主视觉**：承载日间或夜间叙事氛围的清晰背景画面；人物与环境应能被直接辨认，文字可读性由文字区域的局部遮罩保障，而不是用覆盖整幅画面的高强度蒙层换取。
_避免_：几乎不可见的背景纹理、覆盖全画面的深色滤镜

**状态栏详情工具条**：分区详情标题旁用于切换日夜模式和恢复自动选择的界面操作区；它随当前分区保持可见，但不属于该分区的状态数据。
_避免_：顶栏大块模式面板、把模式按钮混入变量卡片

**状态栏折叠层级**：由整份档案、当前分区和分区内信息组构成的三级渐进展示；桌面只默认展开当前最重要的信息组，窄屏默认只展示各组标题、数量与摘要。
_避免_：一次平铺全部卡片、仅依靠整份档案的单层开关

**状态栏开合表面**：状态栏场景头部与常驻概览中可用于开合整份详情的非控件区域；头像、日夜切换、标签、筛选和分页等内部控件拥有独立操作，不触发整栏开合。
_避免_：额外的“展开档案”按钮、点击任意内部控件都收起状态栏

**状态栏头像库**：由用户在浏览器本地维护的姓名与头像映射，同名人物可跨聊天复用，并允许当前聊天单独覆盖；它属于界面个性化信息，不属于人物档案或状态协议。
_避免_：人物资料库、自动上传的公开图库

## 创角向导词汇

**创角向导**：用于在聊天初始化前逐页收集并审阅主角档案的交互流程；它可以服务原作人物或原创角色，但不改变主角档案在当前聊天中的唯一性。
_避免_：角色编辑器、玩家档案页

**剧情锚点**：由一个卷、一个事件节点及其项目时间组成的创角起始位置；它描述角色从哪一段已整理剧情进入世界，不等同于死亡回归存档点。
_避免_：出生点、玩家存档、检查点

**事件时间**：事件索引中的规范日期、时段、时间层和编辑/历史标记的组合；它必须保留“编辑演算”或“历史估算”等资料性质，不能伪装成原作明示日期。
_避免_：现实时间、官方时间戳

## 创角界面词汇

**开局页**：创角向导进入前的 Re:Zero 游戏式入口，负责开始新档、继续草稿、导入导出和设置；它不是角色档案编辑页。
_避免_：把开局页称为角色编辑器

**模型目录**：由酒馆当前连接或独立 OpenAI-compatible 接口返回的完整模型 ID 集合；目录不静默过滤，允许搜索、选择和手工输入。
_避免_：只显示一个默认模型、把模型目录当作角色数据

**帮填预览**：AI 根据当前页或整份草稿提出的局部补丁预览；用户确认后才写入，已填写字段和剧情锚点不会被覆盖。
_避免_：让 AI 直接重写整张角色档案

**角色肖像卡**：始终可见的角色视觉档案区域，展示肖像、姓名、外貌摘要与当前身份；它是创角向导的派生视图，不是 ZOD 状态字段本身。
_避免_：用全屏背景图冒充肖像卡

**Re:Zero 开局语汇**：以魔女茶会、死亡回归、重新选择和档案记录为视觉与文案锚点；参考包只借鉴交互结构，不借用其作品主题或素材。
_避免_：直接复制参考包的标题、色板或世界观

**开局音乐**：开局页顶部工具区可控的循环背景音乐；默认偏好为开启，用户点击音乐按钮后开始或恢复播放，切换到创角页面时保持播放状态。
_避免_：把音乐状态写入主角档案、把浏览器拦截无手势播放视为错误、使用 GitHub 文件展示页作为音频源

**音乐播放偏好**：仅保存在当前浏览器本地的播放开关与音量偏好，不属于创角草稿、状态协议或角色档案；浏览器禁止自动播放时仍保留“待播放”状态，等待用户手势。
_避免_：把播放偏好发送给模型或酒馆、为了自动播放绕过浏览器策略
