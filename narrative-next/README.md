# narrative-next

`narrative-next/` 是正文美化 v2 的维护源目录。`dist/regex-Re0·正文美化-v2.json` 是由这里生成的交付产物，不直接维护。

## 边界

- 旧版 `narrative/` 在切换验收通过前保持不变。
- 解析器与 UI 核心内嵌在 Regex 导入包中；远程只加载不可变的图片和字体资源。
- Phase A 有且只有 8 套已完成人物气泡皮肤和 7 套已完成标题配方，用于视觉验收。
- Phase A 是候选版本，不是完整扩展的最终版本。
