# 星屑碎片状态栏复刻版运行时交接

## 已验证

- 纯模型：六个导航页各自固定六槽位，字段描述直接来自现有 172 叶路径映射。
- 浏览器预览：全屏 16:9 舞台、左侧导航、人物轨道、生成六格主视觉、碎片点击、右侧详情态、返回箭头、激活胶囊和 UID 均已观察。
- 最终脚本包：`preview/bundle.html` 可直接启动 `type: "script"` 产物，开发 UI 与生产包结构一致。

## 真实 SillyTavern 待验收

1. 导入最终脚本并确认宿主层只出现一个悬浮球；脚本热重载不会重复挂载。
2. 在 16:9 桌面窗口对照参考图检查导航比例、人物轨道、六个多边形窗口、编号圆标、右上角入口和左下 UID。
3. 点击每个导航项、人物头像和六个碎片：主界面保持舞台，详情态隐藏左侧按钮/人物轨道并从右侧滑入；返回箭头只退出详情态。
4. 检查 `VARIABLE_INITIALIZED`、`VARIABLE_UPDATE_ENDED`、编辑消息、swipe、收到新消息和切聊天后的状态刷新，确认六槽位内容来自正确消息楼层。
5. 验证艾米莉亚、蕾姆、菜月昴六格素材和其他 NPC registry 头像的加载失败降级；未知人物显示首字或通用背景。
6. 验证主角头像上传只写入浏览器本地，Escape、Tab、焦点返回、缩放和 reduced-motion 不破坏舞台。

自动化和离线预览不能替代真实宿主证据；完成上述清单前，不标记为“真实 SillyTavern 已验收”。
